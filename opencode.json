{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "firebase": {
      "type": "local",
      "command": ["npx", "-y", "firebase-mcp", "--config", "firebase-mcp.json"],
      "enabled": true
    }
  }
}
